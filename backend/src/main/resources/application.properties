spring.application.name=backend
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/wolguplab}
spring.datasource.username=${DATABASE_USERNAME:wolguplab}
spring.datasource.password=${DATABASE_PASSWORD:wolguplab}
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Seoul
